import React from 'react'

const ConversationStarter = ({ text }) => {
    return (
        <div classname="conversation-starter">
            <p>{text}</p>
        </div>
    );
};

export default ConversationStarter